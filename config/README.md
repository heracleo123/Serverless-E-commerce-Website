# Configuration Files

This folder contains all configuration files for the ElectroTech application.

## Files

- **appConstants.js** - Application constants, API endpoints, Cognito configuration
- **Products.json** - Product database seed data

## Usage

Import constants in components:
```javascript
import { APP_CONFIG, CATEGORIES, UI_STRINGS, COLORS } from '../../config/appConstants';
```
